build:
  docker:
  web: Dockerfile-django
  celery: Dockerfile-django
run:
  web: gunicorn djangoproxy.wsgi:application --bind 0.0.0.0:$PORT
  celery: celery -A djangoproxy worker -B --loglevel=INFO --concurrency=10